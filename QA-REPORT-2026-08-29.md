# QA report — 2026-08-29 02:36 KST

범위: 최근 24시간 커밋이 있는 `heightcue-autopilot`, `OpenMontage`. 구현 파일은 수정하지 않음. 작업 트리의 기존 미커밋 변경은 보존함.

## 판정

**FAIL / 릴리스 보류.** typecheck는 통과했지만 관련 테스트 실패 2건과 heightcue 전체 회귀 8건이 확인됨. 실제 브라우저 E2E는 Backlot 프로젝트 페이지까지 접근했으나, 플레이어/렌더 영상이 노출되지 않음.

## 실행 결과

### heightcue-autopilot

명령:

```bash
cd /Users/leeheungkyu/heightcue-autopilot
.venv/bin/python -m pytest autopilot -q
```

결과: **927 passed, 8 failed, 51 subtests passed** (27.71s)

재현 1 — comments 디스패치:

```bash
.venv/bin/python -m pytest autopilot/test_video_run.py::TestExistingCommandsUnchanged::test_comments_dispatches -q
```

관찰 결과: `autopilot/run.py:868`에서 `state_path(cfg, "comments.lock")` 호출 시 `KeyError: 'state_dir'`.
최소 증거: 테스트 fixture의 cfg는 `{'mode': {'dry_run': True}, 'threads': {}, 'paths': {}}`이며, `common.py:23`은 `cfg["paths"]["state_dir"]`를 직접 읽음.

재현 2 — 전체 관련 테스트:

```bash
.venv/bin/python -m pytest autopilot/test_ops.py autopilot/test_video_run.py -q
```

결과: **78 passed, 1 failed, 12 subtests passed**. 위 `test_comments_dispatches` 동일 실패.

전체 8건의 실패 중 나머지:
- `test_codex_image_bridge.py` 4건: `/Users/leeheungkyu/.hermes/profiles/subin-qa/hermes-agent/...`의 Hermes plugin/dispatcher/venv 경로가 존재하지 않음.
- `test_ops.py` 3건: `make_reply`/`publish_text`가 앞선 테스트의 monkeypatch된 lambda를 참조해 TypeError 또는 `'X'`를 반환. 전체 suite 순서에서 재현됨.

### OpenMontage

명령:

```bash
cd /Users/leeheungkyu/OpenMontage
.venv/bin/python -m pytest -q
```

결과: **1861 passed, 1 failed, 12 skipped, 3 xfailed, 1 subtest passed** (65.99s)

재현:

```bash
.venv/bin/python -m pytest tests/tools/test_new_video_model_support.py::test_fal_gemini_omni_and_minimax_h3_are_discovered_and_submit -q
```

관찰 결과: 성공 응답 자체는 반환되지만 endpoint assertion 실패.
- 실제 관찰: `https://queue.fal.run/minimax/h3-max/text-to-video`
- 테스트 기대: `/fal-ai/minimax/hailuo-03/text-to-video`
- 실패 위치: `tests/tools/test_new_video_model_support.py:124`

즉, 최근 구현이 text/image-to-video를 H3 Max로 라우팅하도록 바뀌었는데, 현재 테스트 기대값이 구 라우팅을 유지하고 있음. 테스트를 임의 수정하지 않고 실패로 기록함.

Typecheck:

```bash
cd /Users/leeheungkyu/OpenMontage/remotion-composer
npx tsc --noEmit
```

결과: **통과(exit 0)**.

Diff hygiene:

```bash
cd /Users/leeheungkyu/heightcue-autopilot
.venv/bin/python -m compileall -q autopilot
git diff --check
```

결과: **통과**.

Lint:

- 양 레포 `.venv/bin/ruff`는 존재하지 않음(exit 127).
- PATH의 global `ruff`로 실행 시 heightcue 전체 24건, OpenMontage 대상 범위 포함 전체 157건이 보고됨. 변경 파일 전용 venv lint를 실행할 수 없는 환경이며, 기존 코드/테스트의 baseline 위반도 다수 포함됨.

## 실제 브라우저 E2E (Aside CLI)

사용 명령:

```bash
aside --account u0 exec "Open http://localhost:4750 ... read-only ..."
```

관찰 절차:

1. `http://localhost:4750/` Backlot Library 접속 성공.
2. `HEIGHTCUE_RUN 18 DDROPS US` 카드 확인.
3. 프로젝트 열기: `http://localhost:4750/p/heightcue_run-18-ddrops-us`.
4. 제목: `Backlot — Heightcue_Run 18 Ddrops Us`.
5. 상태: `unknown pipeline`, `IDLE · 7H AGO`.
6. stages: research, proposal, idea, script, scene_plan, assets, edit, compose, publish.
7. 페이지 메시지: `No pipeline state. This project has no checkpoints`.
8. DOM 확인: `videoCount: 0`, `canvasCount: 0`, `imgCount: 0`; 재생 가능한 영상과 오류 메시지 모두 없음.

판정: 브라우저 서버와 Backlot 페이지는 열리지만, 최근 HeightCue UGC 결과를 실제 브라우저에서 재생/검증할 수 있는 상태가 아님. E2E acceptance **실패(검증 불가)**.

참고로 별도 `http://localhost:3000/`는 FAMO landing page로 열렸으며 HeightCue 기능과 무관함.

## 구현자 재현용 요약

1. `cd /Users/leeheungkyu/heightcue-autopilot && .venv/bin/python -m pytest autopilot -q`
2. `cd /Users/leeheungkyu/OpenMontage && .venv/bin/python -m pytest -q`
3. `cd /Users/leeheungkyu/OpenMontage/remotion-composer && npx tsc --noEmit`
4. Backlot 서버가 실행 중이면 Aside로 `http://localhost:4750/` → `HEIGHTCUE_RUN 18 DDROPS US` → project 상세 진입

구현 수정은 하지 않았으며, 본 파일만 QA 증거 산출물로 추가함.
