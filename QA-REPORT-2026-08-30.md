# 야간 QA 보고서 — 2026-08-30

## 판정

**FAIL / 릴리스 보류**

최근 24시간 변경이 확인된 `heightcue-autopilot`과 `OpenMontage`를 검증했다. 구현 파일은 수정하지 않았다. 기존 작업 트리 변경은 보존했다.

## 변경 범위

- `heightcue-autopilot`: 2026-08-29 15:32 이후 커밋 11개, 주요 최근 커밋 `3ba9d5f feat: animate real product photos instead of generating them`; 미커밋 변경도 다수 존재.
- `OpenMontage`: 2026-08-29 15:36 이후 커밋 5개, 주요 최근 커밋 `0b3e5fd feat: animate real product photos instead of generating them`; 미커밋 변경은 `tools/video/video_compose.py`와 관련 테스트.

## heightcue-autopilot

### 테스트

```bash
.venv/bin/python -m pytest autopilot -q
```

결과: **1 failed, 1183 passed, 77 subtests passed** (28.13s)

최단 재현:

```bash
.venv/bin/python -m pytest \
  autopilot/test_fal_upload.py::TestFrameAdapter::test_adapter_output_satisfies_build_cut_request -q
```

관찰 결과:

```text
TypeError: build_cut_request() got an unexpected keyword argument 'motion_prompt'
autopilot/test_fal_upload.py:354
```

원인 증거: 현재 `autopilot/video_generate.py`의 `build_cut_request` 시그니처는 `generation_prompt`를 요구하도록 변경됐지만, 해당 테스트는 여전히 `motion_prompt`를 전달한다. 최근 product fidelity/video generation 변경과 직접 연관된 계약 불일치다.

### 정적 검증

```bash
.venv/bin/python -m compileall -q autopilot
```

통과.

```bash
ruff check autopilot
```

실패: **133 errors**. 신규/변경 범위에서 확인된 대표 오류는 `autopilot/benchmark.py`·`generation_worker.py`의 미사용 import 및 다중 문장, `execution_contract.py`의 E402/E701/E702, `health.py`·`post_check.py`의 모호한 변수명 등이다. 전체 결과는 baseline 오류가 섞여 있어 변경분 단독 판정은 제한된다.

```bash
git diff --check
```

통과.

## OpenMontage

### 테스트

```bash
.venv/bin/python -m pytest -q
```

결과: **1864 passed, 12 skipped, 3 xfailed, 1 subtest passed** (65.26s)

관련 변경 테스트 포함 전체 테스트는 통과했다.

### Typecheck

```bash
cd remotion-composer && npx tsc --noEmit
```

통과.

### Lint / compile

```bash
ruff check tools/video/video_compose.py \
  tests/tools/test_new_video_model_support.py \
  tests/tools/test_video_compose_subtitles.py
```

실패: **4 errors** (`video_compose.py`의 F541 2건, F841 1건, F401 1건).

```bash
python3 -m compileall -q tools tests
git diff --check
```

통과.

## 실제 브라우저 E2E — Aside CLI

`aside --account u0 exec`로 `http://localhost:4750/` Backlot을 읽기 전용으로 확인했다.

- Library: 6 projects, 전체 상단 상태 `IDLE`.
- 최신 `HEIGHTCUE_RUN 28 DDROPS US` 진입 성공.
- 프로젝트 상태: `unknown pipeline`, `IDLE · 47M AGO`.
- 단계 rail: research → proposal → idea → script → scene_plan → assets → edit → compose → publish.
- 모든 단계 checkpoint 상태 없음.
- 빈 상태 문구: `No pipeline state. This project has no checkpoints — Backlot is showing what it found on disk.`
- Activity에는 `transcriber ✓`, `frame_sampler ✓`, `audio_energy ✓`만 표시.
- `video`: 0, `canvas`: 0, `img`: 0.
- 숨겨진 `<audio id="player"></audio>` 1개만 있고 `src`/controls 없음.
- 렌더 미리보기와 재생 가능한 영상이 없으며, 표시된 오류 메시지도 없음.

결론: 최신 HeightCue 실행 결과를 브라우저에서 재생·검증할 수 없어 실제 E2E acceptance는 **실패**다. `REPLAY RUN`은 외부 상태를 바꿀 수 있어 실행하지 않았다.

## 최종 조치

릴리스 보류. @junho-builder 이준호 구현개발 선임은 `build_cut_request` 호출 계약(`motion_prompt` 대 `generation_prompt`)과 lint 오류를 확인해야 한다. OpenMontage 테스트는 통과했지만 lint 및 실제 Backlot 렌더 검증은 통과하지 못했다.
