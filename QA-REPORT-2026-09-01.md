# 야간 QA 보고서 — 2026-09-01

## 범위·판정

- 기준 시각: 2026-09-01 02:30~02:42 KST
- 최근 24시간 커밋은 두 저장소 모두 확인되지 않아, 현재 작업 트리의 미커밋 구현 변경을 검증 대상으로 삼았다.
- 구현 파일은 수정하지 않았다.
- 최종 판정: **FAIL / 릴리스 보류** — HeightCue 변경 파일 lint 오류 8건이 남아 있다.
- 브라우저 렌더 로딩·실제 재생은 이번 실행에서 통과했다.

## 1. heightcue-autopilot

### 회귀 테스트

```bash
.venv/bin/python -m pytest autopilot -q
```

결과: **1324 passed, 93 subtests passed** (40.50s)

### 정적 검증

```bash
.venv/bin/python -m compileall -q autopilot
git diff --check
ruff check $(git diff --name-only -- '*.py')
```

- compileall: 통과
- git diff --check: 통과
- 변경 Python 파일 lint: **실패, 8 errors**

재현:

```bash
cd /Users/leeheungkyu/heightcue-autopilot
ruff check $(git diff --name-only -- '*.py')
```

오류:

- `autopilot/codex_image_bridge.py:31` F401 — unused `sys`
- `autopilot/health.py:121:20` E741 — ambiguous variable `l`
- `autopilot/health.py:124:33` E741 — ambiguous variable `l`
- `autopilot/health.py:135:49` E741 — ambiguous variable `l`
- `autopilot/test_monitor_video_publish.py:17` F401 — unused `json`
- `autopilot/video_handoff.py:54:30` F401 — unused `STATE_DEAD_LETTER`
- `autopilot/video_handoff.py:54:49` F401 — unused `STATE_GENERATING`
- `autopilot/video_handoff.py:55:65` F401 — unused `STATE_QUEUED`

## 2. OpenMontage

### 회귀 테스트

```bash
.venv/bin/python -m pytest -q
```

결과: **1864 passed, 12 skipped, 3 xfailed, 1 subtests passed** (68.48s)

### 정적 검증

```bash
python3 -m compileall -q tools tests
cd remotion-composer && npx tsc --noEmit
cd ..
git diff --check
ruff check tools/video/video_compose.py \
  tests/tools/test_new_video_model_support.py \
  tests/tools/test_video_compose_subtitles.py
```

결과: compileall, TypeScript typecheck, diff check, 변경 범위 lint 모두 통과 (`All checks passed!`).

## 3. 실제 브라우저 E2E — Aside CLI

읽기 전용으로 `aside --account u0 exec` 및 `aside --account u0 repl`을 사용했다. Replay/run/publish/delete는 누르지 않았다.

대상: `HEIGHTCUE_RUN 22 DDROPS US`

URL:

```text
http://localhost:4750/p/heightcue_run-22-ddrops-us
```

관찰:

- Backlot 목록에서 최신 렌더 HeightCue 프로젝트로 표시됨
- 상태: `unknown pipeline`, `IDLE · 1D AGO`
- `No pipeline state. This project has no checkpoints`
- 렌더: 2개 (`job-22-ddrops-us_subtitled.mp4`, `job-22-ddrops-us_clean-master.mp4`)
- video element: 1개
- `currentSrc`: `http://localhost:4750/media/heightcue_run-22-ddrops-us/renders/job-22-ddrops-us_subtitled.mp4`
- 초기 `readyState: 4`, `duration: 15.061333`, `paused: true`, `currentTime: 0`, `error: null`
- DOM locator의 영상 내부 클릭은 재생을 시작하지 않았음
- 화면에서 네이티브 재생 버튼을 실제 좌표로 클릭한 뒤 0.9초 read-back:
  - `paused: false`
  - `currentTime: 0.862763`
  - `readyState: 4`
  - `error: null`
- 후속 독립 Aside REPL read-back에서는 영상이 끝까지 진행되어 `currentTime: 15.061333`, `paused: true`, `error: null` 확인

판정: **브라우저 E2E 재생 통과**. 실제 화면 조작으로 재생이 시작되고 정상 종료됐다. 단, Backlot은 여전히 checkpoint state를 표시하지 않아 운영 관측성 잔여 위험이 있다.

## 후속 조치

- 이준호(@junho-builder): HeightCue 변경 범위의 lint 8건을 수정 후 재검증 필요.
- 릴리스는 lint 오류 해소와 재검증 전까지 보류한다.
