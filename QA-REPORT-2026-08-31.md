# 야간 QA 보고서 — 2026-08-31

## 범위·판정

- 기준 시각: 2026-08-31 08:07 KST
- 최근 24시간 `git log` 커밋은 두 저장소 모두 없었습니다.
- 대신 현재 작업 트리의 미커밋 변경을 최근 구현 후보로 독립 검증했습니다. 구현 파일은 수정하지 않았습니다.
- 최종 판정: **FAIL / 릴리스 보류**. HeightCue 변경분 lint 오류와 브라우저 재생 조작 실패가 남아 있습니다.

## heightcue-autopilot

### 테스트

```bash
.venv/bin/python -m pytest autopilot -q
```

결과: **1219 passed, 77 subtests passed** (30.02s)

### 정적 검증

```bash
python3 -m compileall -q autopilot
 git diff --check
```

결과: 통과.

변경된 Python 파일만 대상으로 한 lint:

```bash
ruff check $(git diff --name-only -- '*.py')
```

결과: **8 errors**

- `autopilot/codex_image_bridge.py:31` F401 unused `sys`
- `autopilot/generate.py:10` F401 unused `re`
- `autopilot/post_check.py:150` E741 ambiguous variable `l`
- `autopilot/test_monitor_video_publish.py:17` F401 unused `json`
- `autopilot/test_ops.py:94` E702 multiple statements on one line
- `autopilot/video_handoff.py:54-55` F401 unused contract imports 3건

전체 `autopilot` lint는 **139 errors**이며 기존·미커밋 신규 파일 baseline이 섞여 있습니다.

## OpenMontage

### 테스트

```bash
.venv/bin/python -m pytest -q
```

결과: **1864 passed, 12 skipped, 3 xfailed, 1 subtests passed** (73.27s)

### 정적 검증

```bash
python3 -m compileall -q tools tests
cd remotion-composer && npx tsc --noEmit
git diff --check
```

결과: 모두 통과.

변경 범위 lint:

```bash
ruff check tools/video/video_compose.py \
  tests/tools/test_new_video_model_support.py \
  tests/tools/test_video_compose_subtitles.py
```

결과: **All checks passed**.

변경 테스트는 MiniMax H3 Max endpoint `/minimax/h3-max/text-to-video`와 `768P` resolution을 검증합니다.

## 실제 브라우저 E2E — Aside CLI

Aside(`aside --account u0 exec`)로 Backlot을 읽기 전용 확인했습니다.

대상: `HEIGHTCUE_RUN 22 DDROPS US` (`http://localhost:4750/p/heightcue_run-22-ddrops-us`)

관찰:

- 상태: `IDLE · 15H AGO`
- `unknown pipeline`
- `No pipeline state. This project has no checkpoints`
- 렌더 2개가 목록에 표시됨
- `<video controls preload="metadata">` 1개, non-empty source:
  `/media/heightcue_run-22-ddrops-us/renders/job-22-ddrops-us_subtitled.mp4`
- `currentSrc`가 localhost media URL로 해석됨
- `readyState: 4`, `duration: 15.061333`, `error: null`
- 재생 버튼/영상 표면/키보드 조작을 실제로 시도했으나 `paused: true`, `currentTime: 0`이 유지됨
- Aside 세션은 180초 timeout으로 종료됨. Replay/run/publish/delete는 누르지 않았습니다.

따라서 파일 로딩 자체는 확인했지만, 실제 브라우저에서 재생 acceptance는 **실패(재생 시작 불가)**로 판정합니다. 네이티브 컨트롤/오버레이와 재생 이벤트 경로를 재현해 수정·재검증해야 합니다.

## 후속 조치

- `@junho-builder` 이준호: HeightCue 변경분 8개 lint 오류와 브라우저 재생 불가 원인 확인 필요.
- OpenMontage는 이번 변경 범위의 테스트·typecheck·lint는 통과했으나, HeightCue 릴리스 보류 판정은 유지합니다.
